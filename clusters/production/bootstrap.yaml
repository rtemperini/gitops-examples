apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bootstrap-prod
spec:
  project: default

  source:
    repoURL: "https://github.com/rtemperini/gitops-examples.git"
    targetRevision: master
    path: apps/production/bootstrap

  destination:
    server: "https://kubernetes.default.svc"
    namespace: argocd

  syncPolicy:
    automated:
      prune: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 10m

